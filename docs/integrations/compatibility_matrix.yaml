# Matrice de compatibilité inter-librairies
# Mise à jour par le Release Manager lors de chaque validation d'intégration.

library: baobab-auth-database
version: v0.2.0

validated_consumers: []

validated_dependencies:
  - name: baobab-auth-core
    version: v0.5.1
    status: PASSED
    integration_method: pypi
    ref: "0.5.1"
    validation_type:
      - import_tests
      - contract_tests
      - catalog_diagnostics_tests
    report: docs/integrations/reports/baobab-auth-core-v0.5.1__baobab-auth-database-v0.2.0.md
    validated_features:
      - DefaultAuthCatalog import and seed
      - catalog check seed report CLI
      - auth_catalog_versions persistence
      - deterministic catalog checksum
      - SERVICE role without permissions
      - SUPER_ADMIN all permissions from core
    notes: >
      Profil core_051 ; migration 0002 ; diagnostics read-only ;
      validation CLI autonome (baobab-auth-admin indisponible).
