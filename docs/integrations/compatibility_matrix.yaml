# Matrice de compatibilité inter-librairies
# Mise à jour par le Release Manager lors de chaque validation d'intégration.

library: baobab-auth-database
version: v0.1.1
pypi_version: "0.1.1"

validated_consumers: []

validated_dependencies:
  - name: baobab-auth-core
    version: v0.5.1
    status: PASSED
    integration_method: pypi
    ref: "0.5.1"
    validation_type:
      - import_tests
      - contract_tests
      - integration_tests
    report: docs/integrations/reports/baobab-auth-core-v0.5.1__baobab-auth-database-v0.1.1.md
    notes: >
      Ports UserRepository, RoleRepository, PermissionRepository,
      SessionRepository, AuditRepository, UnitOfWork ; bootstrap
      DefaultAuthCatalog idempotent ; CLI migrations.
